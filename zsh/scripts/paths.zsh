#!/bin/zsh

export PATH="/usr/local/bin${PATH+:${PATH}}"
export PATH="${HOME}/.rd/bin${PATH+:${PATH}}"
export PATH="${HOME}/.golang/bin${PATH+:${PATH}}"
export PATH="${HOME}/.nodenv/shims${PATH+:${PATH}}"
export PATH="${HOME}/.pyenv/shims${PATH+:${PATH}}"
export PATH="${HOME}/.jenv/shims${PATH+:${PATH}}"
